package Tablero;

import org.junit.jupiter.api.Test;
import static org.mockito.Mockito.*;
import static org.junit.jupiter.api.Assertions.*;
import java.util.Random;

public class RandomizadorFichaNumeroTest {

    /*
    @Test
    void testReordenaLosNumerosDeManeraAleatoria() {
        // 1️⃣ Crear el mock de Random
        Random randomMock = mock(Random.class);

        // 2️⃣ Definir el comportamiento esperado del mock
        when(randomMock.nextInt(1)).thenReturn(0);

        // 3️⃣ Inyectar el mock en la clase a testear
        RandomizadorFichaNumero generador = new RandomizadorFichaNumero(randomMock);

        // 4️⃣ Ejecutar el método
        int[] resultado = generador.generarNumeros();

        int[] resultadoEsperado = {2, 3, 3, 4, 4, 5, 5, 6, 6, 8, 8, 9, 9, 10, 10, 11, 11, 12};
        // 5️⃣ Verificar el resultado
        assertArrayEquals(resultadoEsperado, resultado);

    }
    */
}